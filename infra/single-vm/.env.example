# =============================================================================
# Environment Configuration - Single VM Deployment
# =============================================================================
# Copy this file to .env and configure your values
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Domain name for SSL certificate (use 'localhost' for local testing)
DOMAIN_NAME=localhost

# Email for Let's Encrypt SSL notifications
CADDY_EMAIL=admin@example.com

# Secret key for JWT tokens (REQUIRED - generate with: openssl rand -base64 64)
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# Database mode: 'local' (PostgreSQL in Docker) or 'remote' (external database)
DATABASE_MODE=local

# PostgreSQL settings (required if DATABASE_MODE=local)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD
POSTGRES_DB=app_db

# Database URL (auto-configured for local mode, set manually for remote)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=https://localhost,http://localhost

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------

# API base URL (relative for same-origin)
VITE_API_BASE_URL=/api/v1
